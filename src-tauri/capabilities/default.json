{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default-capability",
  "description": "Capability for the PI WEB desktop app",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "fs:default",
    "os:default",
    "process:default",
    "shell:default",
    "store:default"
  ]
}
